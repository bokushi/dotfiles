compressed-ssh
compressed
create-dirs
fail
location
parallel
referer ";auto"

# バグ 下記2つは同時に使うことができない
# https://github.com/curl/curl/issues/3512
remote-header-name
# continue-at -

# continue-at を使わない場合、指定した方が良い
remove-on-error

remote-name-all
styled-output
user-agent ""
#no-clobber 同名のファイルを上書きしたくない場合は指定する 適切なpostfixがつくようになる
write-out "%{filename_effective}\n"
