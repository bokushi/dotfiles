# (HTTP) Figure out authentication method automatically, and use the most secure one the remote site claims to support.
anyauth
compressed
#continue-at -
create-dirs

# (HTTP) Fail with error code 22 and with no response body output at all for HTTP transfers returning HTTP response codes at 400 or greater.
fail

# This is an alternative option to --fail which makes curl fail for the same circumstances but without saving the content.
#fail-with-body

location
referer ";auto"
netrc-optional

parallel
parallel-max 65535

#mptcp
http3

#continue-at or no-clobber

remote-header-name
#remote-name-all

#ssl-auto-client-cert

#ssl

styled-output

tcp-fastopen

#trace-ids

user-agent ""
write-out "%{filename_effective}\n"
